import 'package:flutter_test/flutter_test.dart';

// Top-level widget smoke test. Full widget tests are in test/features/.
void main() {
  testWidgets('app smoke test placeholder', (tester) async {
    // Will be replaced with a real app smoke test once Firebase is configured.
    expect(1 + 1, equals(2));
  });
}
